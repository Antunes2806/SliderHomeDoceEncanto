import { StyleSheet } from "react-native";

export const StylesCarrinho = StyleSheet.create({
  footer: {
    width: 10, // Define a largura do rodapé
    backgroundColor: "red", // Define a cor do rodapé
  },
});
